import { useEffect, useState } from 'react';
import useAxios from './hooks/useAxios/useAxios';

function App() {
  return (
    <div className='App'>
      <h1>Playground</h1>
    </div>
  );
}

export default App;
